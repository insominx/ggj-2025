using UnityEngine;

public class RealityMixer : MonoBehaviour
{
    // Editor/data/prefab interface.
    public GameObject floor;

    void Start()
    {
        // Procedurally create a floor.
    }

    // Update is called once per frame.
    void Update()
    {
    }
}
